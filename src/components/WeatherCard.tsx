import React from "react"

export const WeatherCard: React.FunctionComponent = () => {
    return (
        <div className='weather-card'>
            <p>Fill in all the fields and the weather will be displayed</p>
        </div>
    )
}

